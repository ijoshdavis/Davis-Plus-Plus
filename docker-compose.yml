services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: kinstore
      POSTGRES_DB: kinstore
    ports:
      - "5488:5432"
    volumes:
      - kinstore-pg-data:/var/lib/postgresql/data

volumes:
  kinstore-pg-data:
